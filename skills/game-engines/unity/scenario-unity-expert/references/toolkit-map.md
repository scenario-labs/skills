# Toolkit map: the Unity call behind every `ut_*` function and AgentKit job

Why this file exists: a plan written as `ut_run.run_method(...)` or `AgentCapture.CaptureViews` cannot be checked by a reader who does not have `scripts/`. The Unreal team's first blind test lost executability points for exactly that. Copy the engine call next to each toolkit call, in plans, handoffs and reports.

Status: every row marked **ran** was executed in Unity 6000.3.21f1 on macOS 26.5.1 (Apple Silicon, Metal) on 2026-09-24 through `tests/code/unity-expert/` (results in `archive/tests/unity-expert/live_results.jsonl`). **not run** rows say why. v0.2 (refactor round 1, same day): grace timeout and `scan_log` fix in `ut_run`, explicit null paths in `AgentJob`, 2D-light and per-scene warm-up in `AgentCapture`, a `totalSize` note in `build_summary`; tested by `test_live_fixes.py` and `test_offline.py::TestV02Fixes`. Every v0.1 signature and return value is unchanged.

## 1. Report format (every plan, handoff and final report)

1. **Each step names its engine call.** Write the toolkit call, then in parentheses the Unity API, command line or setting it uses. Example:
   - `ut_run.run_method(P, "AgentKit.AgentCapture.CaptureViews", {...}, graphics=True)` (`Unity -batchmode -quit -projectPath P -executeMethod AgentKit.AgentCapture.CaptureViews -agentJob <dir>`; the job calls `RenderPipeline.SubmitRenderRequest(cam, new RenderPipeline.StandardRequest{destination = rt})`, then `Texture2D.ReadPixels` and `EncodeToPNG`)
   - `ut_run.run_tests(P, "PlayMode")` (`Unity -batchmode -nographics -projectPath P -runTests -testPlatform PlayMode -testResults <abs>.xml`, no `-quit`)
   - Settings are written as the property and asset they live in: `UniversalRenderPipelineAsset.shadowDistance = 40` in `Assets/Settings/PC_RPAsset.asset`, never "shorten the shadows".
2. **Menus are written as paths** for a human or computer-use agent: `File > Build Profiles`, `Window > Analysis > Profiler`.
3. **End with two lists.**
   - **Verified**: what ran, on which channel, and the evidence (envelope `ok` with its job id, `image_checks` flags and the contact sheet you looked at, `budget_check` verdict, audit counts, NUnit totals, BuildReport result and size).
   - **Assumed**: every step not run in Unity, every `[verify]` name, every default taken because the brief was silent, every number quoted from a source and not re-measured.
4. A step you could not run is written as a plan with its engine call, never as a result.

## 2. `ut_env` (editor, project, license, lock rule)

| Toolkit call                               | Unity call or command                                                                                                                                                                                                               | Status                                                                   |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `find_editor(version="6000.3.21f1")`       | `/Applications/Unity/Hub/Editor/<v>/Unity.app/Contents/MacOS/Unity`; modules from `Contents/PlaybackEngines` (macOS) and `<v>/PlaybackEngines` (Hub modules: AndroidPlayer, iOSSupport, WebGLSupport); `UNITY_EDITOR` env overrides | ran                                                                      |
| `license_ok()`                             | reads `<UpdateDate>` in `~/Library/Unity/licenses/UnityEntitlementLicense.xml` (end of the 30-day offline window of a Personal license); no secret read                                                                             | ran (valid to 2026-10-24)                                                |
| `find_project(path)`                       | walks up to `Assets/` + `ProjectSettings/ProjectVersion.txt`; reads `Packages/manifest.json`                                                                                                                                        | ran                                                                      |
| `base_project(kind, dest)`                 | `cp -cR tests/projects/Base3D_URP                                                                                                                                                                                                   | Base2D_URP <dest>`(APFS clone), then`install_agentkit`                   | ran (this skill's project) |
| `new_project(path, template)`              | `Unity -batchmode -nographics -quit -createProject <path> -cloneFromTemplate <Editor>/Contents/Resources/PackageManager/ProjectTemplates/com.unity.template.3d-cross-platform-17.0.14.tgz`                                          | not run here (the base projects were made this way; minutes of import)   |
| `install_agentkit(project)`                | copies `scripts/AgentKit/**/*.cs` to `Assets/Editor/AgentKit/` (no asmdef: compiles into Assembly-CSharp-Editor)                                                                                                                    | ran                                                                      |
| `project_lock(project)`, `assert_unlocked` | `ps -axo pid=,command=` for Unity processes whose `-projectPath` is the project; non-blocking `fcntl.lockf` probe on `Temp/UnityLockfile`                                                                                           | ran (a resident editor was detected by both: process and held lock file) |
| `preflight(project)`                       | the above plus `xcodebuild -version`                                                                                                                                                                                                | ran                                                                      |

## 3. `ut_run` (batch channel)

| Toolkit call                                                                                                                        | Unity call or command                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Status                                                                                                                                                                                                           |
| ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `run_method(P, "Ns.Class.Method", args, timeout=1800, graphics=False, quit=True, build_target=None, grace=None, result_grace=None)` | `Unity -batchmode [-nographics] [-quit] -projectPath P -logFile <job>/unity.log [-buildTarget T] -executeMethod Ns.Class.Method -agentJob <job>`; args in `<job>/args.json`; result = the `AGENT_RESULT {json}` line or `<job>/result.json`; job dir `P/Library/AgentKit/jobs/<id>/`                                                                                                                                                                                                             | ran: 6 to 13 s per job on this project                                                                                                                                                                           |
| grace timeout (v0.2, inside `run_method` and `build`)                                                                               | follows `<job>/unity.log` once a second; after this job's `AGENT_RESULT` plus a shutdown line (`Batchmode quit successfully invoked`, `Exiting batchmode successfully now!`, `Cleanup mono`...) and `grace` s (default 10, `UT_GRACE_S`) of silence, or after the result alone and `result_grace` s (default 120, `UT_RESULT_GRACE_S`): `kill -TERM <pid>`, `-KILL` 15 s later, that PID only; `out["grace_kill"]` = `{killed, reason, pid, after_s}`, the envelope stays the result; 0 disables | ran (two runs): hang in `EditorApplication.quitting` ended at 15 to 17 s (`grace=5`), main-thread hang at 28 to 33 s (`result_grace=20`), Echo and async jobs untouched                                          |
| `run_method(..., quit=False)`                                                                                                       | same without `-quit`; the job calls `AgentJob.BeginAsync()` and ends with `EditorApplication.Exit(code)`                                                                                                                                                                                                                                                                                                                                                                                         | ran (30-tick job, play-mode profile)                                                                                                                                                                             |
| `run_tests(P, "EditMode"                                                                                                            | "PlayMode", filter=None)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `Unity -batchmode [-nographics] -projectPath P -runTests -testPlatform <p> -testResults <abs>.xml -logFile <log> [-testFilter F] [-testCategory C] [-assemblyNames A]`; parses NUnit3 `test-run` and `test-case` | ran: EditMode 2/2, PlayMode 1/1, exit 0, about 14 s each |
| `build(P, target, profile=None, out=None)`                                                                                          | `Unity -batchmode -nographics -quit -projectPath P -buildTarget <StandaloneOSX                                                                                                                                                                                                                                                                                                                                                                                                                   | WebGL                                                                                                                                                                                                            | Android                                                  | iOS> -executeMethod AgentKit.AgentBuild.Build -agentJob <job>`(+`-activeBuildProfile <asset>` with a profile) | ran: macOS and Web |
| `scan_log(text)`                                                                                                                    | regexes for `path(l,c): error CSxxxx`, asmdef errors ("Assembly has duplicate references"), exceptions, "Scripts have compiler errors", license refusal, project open elsewhere, method not found, crash; `AGENT_RESULT` and `[AgentKit]` lines are skipped as job output (v0.2: they were reported as ASMDEF errors)                                                                                                                                                                            | ran on real logs                                                                                                                                                                                                 |
| `parse_agent_result(text, job_id)`, `parse_nunit(xml)`, `jobs(P)`                                                                   | pure Python                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | ran                                                                                                                                                                                                              |

## 4. C# AgentKit jobs (copied into `Assets/Editor/AgentKit/`)

| Job or helper                                                                                                                                                                                                                                                                                                                              | Unity APIs it uses                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Status                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `AgentJob.Run(body)`, `Args`, `Has`, `Str/Int/Float/Bool/List/Dict`, `TryList/TryDict/ListOrNull/DictOrNull` (v0.2), `ResolvePath`, `OutDir`, `Warn`, `BeginAsync`, `Succeed`, `Fail`. `List`/`Dict` NEVER return null (empty when missing), so `?? fallback` after them never fires: use `Has`, `TryList`, or `ListOrNull(k) ?? fallback` | `Environment.GetCommandLineArgs` (`-agentJob`, `-executeMethod`, `-quit`), `SessionState` (async context across domain reloads), `Debug.Log` with `Application.SetStackTraceLogType(LogType.Log, StackTraceLogType.None)` for the one-line envelope, `EditorApplication.Exit(0                                                                                                                                                                                                                                                                                                                                                                                                                | 1)`                                                                                                                 | ran        |
| `AgentJob.Echo`                                                                                                                                                                                                                                                                                                                            | `GraphicsSettings.currentRenderPipeline`, `PlayerSettings.colorSpace`, `EditorUserBuildSettings.activeBuildTarget`, `QualitySettings`, `EditorSettings.enterPlayModeOptions*`, `PlayerSettings.GetScriptingBackend(NamedBuildTarget.Standalone)`                                                                                                                                                                                                                                                                                                                                                                                                                                              | ran                                                                                                                 |
| `AgentCapture.CaptureViews` (args `scene`, `views`, `camera`, `width`, `height`, `out_dir`, `scene_bookmarks`, `all_cameras`, `msaa`, `warm_2d_lights`)                                                                                                                                                                                    | `EditorSceneManager.OpenScene`; a hidden copy of the template camera (`Object.Instantiate`, keeps `UniversalAdditionalCameraData`); `RenderPipeline.SupportsRenderRequest` + `RenderPipeline.SubmitRenderRequest(cam, StandardRequest{destination = sRGB RenderTexture})`, fallback `Camera.Render()`; `Texture2D.ReadPixels`, alpha forced to 255, `EncodeToPNG`; one discarded warm-up frame per set of loaded scenes (keyed on `Scene.handle`); before every capture, URP 2D lights warmed through reflection on the internal `Light2D.UpdateMesh(true)` and `UpdateBoundingSphere()` (arg `warm_2d_lights`, default true; result `lights2d_warmed`); helper `AgentCapture.WarmLights2D()` | ran: 3 bookmarks, SubmitRenderRequest path; 2D project: spot light absent cold, center 0.091 to 0.315 linear warmed |
| `AgentCapture.CaptureBlack`                                                                                                                                                                                                                                                                                                                | camera with `cullingMask = 0`, black clear                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | ran (flagged all_black)                                                                                             |
| `AgentCapture.SaveBookmark(name, pos, lookAt, fov)`, `SceneBookmarks()`                                                                                                                                                                                                                                                                    | disabled `Camera` on a GameObject named `AgentView_<name>`; `Resources.FindObjectsOfTypeAll<Camera>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ran                                                                                                                 |
| `AgentCapture.CaptureSceneView(path)`                                                                                                                                                                                                                                                                                                      | `SceneView.lastActiveSceneView.camera` (GUI editor only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | not run (no GUI window during tests)                                                                                |
| `AgentAudit.AuditScene` (args `scene`, `out`)                                                                                                                                                                                                                                                                                              | `SceneManager.GetSceneAt`, `GetRootGameObjects`, `GameObjectUtility.GetMonoBehavioursWithMissingScriptCount`, `PrefabUtility.IsPrefabAssetMissing`, `Renderer.sharedMaterials`, `Shader.isSupported`, `Light.lightmapBakeType`, `Lightmapping.TryGetLightingSettings`, `LightmapSettings.lightmaps`                                                                                                                                                                                                                                                                                                                                                                                           | ran                                                                                                                 |
| `AgentAudit.AuditAssets` (args `folders`, `types`, `max_texture_size`, `platforms`, `out`)                                                                                                                                                                                                                                                 | `AssetDatabase.FindAssets("t:Texture2D                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | t:Model                                                                                                             | t:Material | t:Prefab | t:AudioClip", folders)`; `TextureImporter` (`GetSourceTextureWidthAndHeight`, `textureType`, `maxTextureSize`, `textureCompression`, `mipmapEnabled`, `isReadable`, `sRGBTexture`, `npotScale`, `GetPlatformTextureSettings`); `ModelImporter` (`isReadable`, `meshCompression`, `importBlendShapes`, `animationType`, `generateSecondaryUV`, `globalScale`, `useFileScale`) + mesh bounds from `AssetDatabase.LoadAllAssetsAtPath`; `AudioImporter.defaultSampleSettings`; `PrefabUtility.LoadPrefabContents` | ran on generated assets |
| `AgentBuild.Build` (args `target`, `out`, `profile`, `options`, `development`, `detailed`, `scenes`)                                                                                                                                                                                                                                       | `BuildPipeline.BuildPlayer(BuildPlayerOptions)` or `BuildPipeline.BuildPlayer(BuildPlayerWithProfileOptions)`; `BuildReport.summary` (`result`, `totalSize`, `totalTime`, `totalErrors`, `totalWarnings`), `report.steps[].messages`, `report.GetFiles()`; `PlayerSettings.WebGL.*` for Web; writes `agent_build_report.json`                                                                                                                                                                                                                                                                                                                                                                 | ran: macOS, Web                                                                                                     |
| `AgentBuild.ListProfiles`                                                                                                                                                                                                                                                                                                                  | `AssetDatabase.FindAssets("t:BuildProfile")`, `BuildProfile.GetActiveBuildProfile`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | not run (no profile asset; 6.3 has no public API to create one)                                                     |
| `AgentProfile.PlayModeTimings` (args `scene`, `frames`, `warmup`, `target_fps`, `render`, `width`, `height`, `out_csv`)                                                                                                                                                                                                                    | `EditorApplication.EnterPlaymode()`; `[InitializeOnLoad]` driver resumed from `SessionState` after the play-mode domain reload; `Camera.main.targetTexture = RenderTexture` (offscreen rendering in batch Play mode); `ProfilerRecorder.StartNew(category, name, capacity)` and `CopyTo` for Main Thread, GC Allocated In Frame, GC Reserved Memory, System Used Memory, Draw Calls Count, SetPass Calls Count, Batches Count, Triangles Count, Vertices Count, CPU Total Frame Time, CPU Main Thread Frame Time, GPU Frame Time; `EditorApplication.ExitPlaymode()`                                                                                                                          | ran (Render Thread counter missing in the Editor; GPU Frame Time mostly 0)                                          |
| `AgentBridge` (`Ping`, `Refresh`, `LastCompile`, `SaveAll`, `Quit`)                                                                                                                                                                                                                                                                        | `[InitializeOnLoad]`, `EditorApplication.update` poll of `Library/AgentKit/bridge/inbox`, reflection `MethodInfo.Invoke`, `CompilationPipeline.assemblyCompilationFinished` for compile messages, `AssetDatabase.Refresh(ForceSynchronousImport)` + `CompilationPipeline.RequestScriptCompilation()`, `EditorSceneManager.SaveOpenScenes`, `EditorApplication.Exit` (batch only)                                                                                                                                                                                                                                                                                                              | ran against a resident headless editor                                                                              |

## 5. `ut_live` (live channels)

| Toolkit call                                                                                          | Unity call or command                                                                                                                                                                                                                                                                                                                                                                                     | Status                                                                                                                                                                             |
| ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `start_headless(P, graphics=True)`                                                                    | `Unity -batchmode -projectPath P -logFile P/Library/AgentKit/bridge/headless.log` (no `-quit`, no `-executeMethod`), detached; waits for the bridge heartbeat                                                                                                                                                                                                                                             | ran                                                                                                                                                                                |
| `call(P, method, args)`                                                                               | writes `bridge/inbox/<id>.json`; the editor's `AgentBridge` runs the method through `AgentJob` and writes `bridge/jobs/<id>/result.json`                                                                                                                                                                                                                                                                  | ran                                                                                                                                                                                |
| `eval(P, code, via="auto")`                                                                           | auto: `unity command eval '<code>' --project-path P` (Pipeline, Roslyn in memory) when the CLI reaches the editor; else the bridge: writes `Assets/Editor/AgentEval/AgentEval_<id>.cs`, bridge `Refresh` (`AssetDatabase.Refresh` + `CompilationPipeline.RequestScriptCompilation`), waits for the compile, then `call(... "AgentKit.Eval.E_<id>.Run")`; snippet moved to `Library/AgentKit/eval_archive` | ran both: Pipeline 0.03 to 0.15 s; bridge 3.2 to 5.4 s compile + 1.0 to 2.9 s call                                                                                                 |
| `stop_headless(P)`                                                                                    | bridge `Quit` (`EditorApplication.Exit(0)`), then `kill -TERM <pid>` of that PID only                                                                                                                                                                                                                                                                                                                     | ran                                                                                                                                                                                |
| `channel(P)`                                                                                          | `project_lock` + bridge heartbeat + `unity command --project-path P`                                                                                                                                                                                                                                                                                                                                      | ran                                                                                                                                                                                |
| `find_cli`, `cli(args)`, `cli_status`, `pipeline_list`, `pipeline_install`, `cli_command`, `cli_eval` | `unity <args> --format json` with `UNITY_NO_CONSENT_PROMPT`, `UNITY_NO_UPDATE_CHECK`, `UNITY_NO_CRASH_REPORT`, `UNITY_NO_PAGER`; `unity pipeline install --project-path P --non-interactive [--package-version <v>]`; `unity status`; `unity pipeline list`; `unity command [name] [--param v] --timeout N`; `unity command eval '<C#>'`                                                                  | ran with CLI 1.0.0-beta.11 and com.unity.pipeline 0.7.0-exp.1: install, status (batch editor listed, port 7800), 151 commands, eval, screenshot, open_scene, get_performance_stats |

## 6. `ut_review` and `ut_stat` (no engine call)

| Toolkit call                                                                                                                                                 | What it computes                                                                                                                                                                                                                                                                                                                                       | Status               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------- |
| `image_checks(path)`                                                                                                                                         | Rec. 709 luma histogram (16 bins), mean, median, std, p01, p99; clipped (luma >= 250), channel-clipped, crushed (<= 5) fractions; `magenta_fraction` (error shader pink); `mean_saturation`; EV-like `mean_ev` and `log_avg_ev` of the display-referred frame; center vs surround stops; flags `all_white`, `all_black`, `uniform`, `magenta`, `blank` | ran on real captures |
| `image_verdict`, `compare(a, b)`, `contact_sheet`, `review_images`, `review_capture(envelope)`                                                               | error/warn/info lines; parity (mean and max abs diff, changed fraction, PSNR, identical); tiled sheet to LOOK at                                                                                                                                                                                                                                       | ran                  |
| `ut_stat.read_frame_csv`, `stats`, `budget_check(frames_ms, target_ms)`, `gc_check`, `bound_by`, `summarize_csv`, `compare_frames`, `fps_to_ms(fps, mobile)` | mean, p50, p95, p99, max; over-budget share; hitches (> 2x budget); verdict pass/warn/fail; GPU-vs-CPU bound when GPU time exists                                                                                                                                                                                                                      | ran on a real CSV    |
| `ut_stat.build_summary(report, max_mb)`, `test_summary(xml)`                                                                                                 | normalized BuildReport verdicts (size budget on the output on disk, Web decompression fallback; v0.2: an info line when `totalSize` exceeds 1.5 x the output on disk, the IL2CPP do-not-ship folder); NUnit totals                                                                                                                                     | ran                  |
