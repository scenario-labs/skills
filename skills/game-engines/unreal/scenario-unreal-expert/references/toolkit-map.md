# Toolkit map: the engine call behind every `ue_*` function

Why this file exists: in the first blind test (2026-09-24, `tests/grading/U1_grade.md` to `U9_grade.md`) four of nine graders took the executability point off the skilled answer (U1, U3, U4, U8) and the other five flagged the same dependency as a caveat or a condition of their score: steps written as `ue_run.run_python(...)`, `ue_review.screenshot(...)` or `L.spawn_light(...)` could not be checked by a reader without `scripts/`. A plan is only as checkable as its least explained call. This table is what you copy next to each call.

Status: every in-engine row is **not yet run in Unreal** (UE 5.8 not installed on 2026-09-24); the offline rows ran in `tests/code/unreal-expert/`. `[verify]` = name or flag to confirm with the probe job (`tests/code/unreal-expert/jobs/job_00_probe.py`).

## 1. Report format (every plan, handoff and final report)

1. **Each step names its engine call.** Write the toolkit call, then in parentheses the Unreal API, command line, console command or cvar it runs, from the tables below. Domain helpers (`ue_light`, `ue_world`, `ue_cine`...) follow the same rule: name the `unreal.*` class and method, the property path, or the cvar they set. Example:
   - `ue_run.run_python(up, "audit_job.py", args=["/Game/Props"])` (`UnrealEditor-Cmd <uproject> -run=pythonscript -script=<boot.py>`; the job calls `EditorAssetSubsystem.load_asset` per asset)
   - `ue_review.screenshot(p, 1920, 1080)` (`unreal.AutomationLibrary.take_high_res_screenshot(1920, 1080, p)`, fallback console `HighResShot 1920x1080 filename="p"`)
   - `set r.Lumen.Reflections.MaxRoughnessToTrace 0.3` written as the cvar itself, never "tighten reflections".
2. **Raw console commands and cvars appear literally**, with where they are set: console, `-DPCVars=` at launch, a device profile, or `Config/DefaultEngine.ini` section and key.
3. **End with two lists.**
   - **Verified**: what ran, on which channel, and the number, log line or frame that proves it (`ue_run` envelope `ok` with `reported_by`, `image_checks` flags, `budget_check` verdict, audit verdict).
   - **Assumed**: every step not run in Unreal, every `[verify]` name, every default taken because the brief was silent, every number from a source that was not re-measured here.
4. A step you could not run is written as a plan with its engine call, never as a result.

## 2. `ue_run` (process layer and job protocol)

| Toolkit call                                                                                                                       | Engine call, command or cvar                                                                                                                                                                                                                                                    | Notes                                                                                                                                         |
| ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `run_python(up, job, mode="commandlet")`                                                                                           | `UnrealEditor-Cmd <uproject> -run=pythonscript -script=<job_dir>/boot.py -unattended -nosplash -stdout -FullStdOutLogOutput -abslog=<log>`                                                                                                                                      | no level loaded; `-abslog` [verify]; Mac falls back to the `UnrealEditor` binary with `-run=` (5.8 batch example)                             |
| boot, before the job                                                                                                               | `unreal.AssetRegistryHelpers.get_asset_registry().wait_for_completion()` (fallback `search_all_assets(True)` [added]), then `LevelEditorSubsystem.load_level(map)` when `map=`                                                                                                  | recorded as `registry_wait`, `map_loaded`                                                                                                     |
| `wait_for_registry()` (job side; also in code sent to a just-launched editor through MCP or `PythonRemote`)                        | `AssetRegistryHelpers.get_asset_registry().wait_for_completion()`, fallback `search_all_assets(True)`                                                                                                                                                                           | returns `{"method", "seconds"}`                                                                                                               |
| boot, after the job                                                                                                                | prints `UE_RESULT {"ok", "job_id", "result", "error", "traceback", ...}` and writes `result.json`                                                                                                                                                                               | pass only on this envelope with this job id, never on the exit code                                                                           |
| `run_python(..., mode="editor")`                                                                                                   | `UnrealEditor <uproject> [map] -ExecutePythonScript=<boot.py> -ScriptErrorsAreFatal`                                                                                                                                                                                            | full modules, no ticks (editor quits after the script); flag from the 5.5 release notes                                                       |
| `run_python(..., mode="latent")`                                                                                                   | `UnrealEditor <uproject> [map] -EnablePython -ExecCmds="py <boot.py>"`, then `unreal.register_ticker_callback(tick)`; each `yield` is one tick; ends with `unreal.SystemLibrary.quit_editor()` (fallback console `QUIT_EDITOR`)                                                 | screenshots and renders without a running editor; `-ExecCmds py` timing and `quit_editor` [verify]                                            |
| `run_python(..., strict=True)`                                                                                                     | same command; also fails on a `Fatal` log line, a non-zero exit code (not latent), or `LogPython: Error` lines after an ok envelope                                                                                                                                             | CI setting; CLI `--strict`                                                                                                                    |
| `run_python(..., lock_check="refuse")`, `editor_processes(up)`                                                                     | `ps -axo pid=,command=`: any `UnrealEditor` or `UnrealEditor-Cmd` process with this `.uproject` on its command line                                                                                                                                                             | the open editor holds a write lock (Jamie Dale, 0guOMTiwmhk [01:17:18]); default `"warn"`                                                     |
| `run_commandlet(up, "DataValidation")`                                                                                             | `UnrealEditor-Cmd <uproject> -run=DataValidation`                                                                                                                                                                                                                               | runs C++ validators only unless Python ones register at startup; pass = exit 0, no Fatal, summary not `Failure - N error(s)` [verify wording] |
| `run_commandlet(up, "ResavePackages", ["-fixupredirects", "-autocheckout", "-projectonly"])`                                       | `-run=ResavePackages -fixupredirects -autocheckout -projectonly`                                                                                                                                                                                                                | `-autocheckout` needed under Perforce (redirectors doc)                                                                                       |
| `run_commandlet(up, "WorldPartitionBuilderCommandlet", [map, "-Builder=WorldPartitionHLODsBuilder", "-AllowCommandletRendering"])` | same flags                                                                                                                                                                                                                                                                      | HLOD build                                                                                                                                    |
| `build_buildcookrun(uat, up)` + `run_uat(cmd[1:])`                                                                                 | `RunUAT.sh BuildCookRun -project=<up> -platform=Mac -clientconfig=Development -unattended -utf8output -build -cook -stage -pak -package -iostore [-archive -archivedirectory=<dir>] -additionalcookeroptions="-RunAssetValidation -RunMapValidation -ValidationErrorsAreFatal"` | `-iostore`, `-archivedirectory`, `-additionalcookeroptions` [verify]; profile Development or Test                                             |
| `launch_editor(up, mcp_port=8000)`                                                                                                 | `UnrealEditor <uproject> -ModelContextProtocolStartServer -ModelContextProtocolPort=8000`                                                                                                                                                                                       | Unreal MCP doc                                                                                                                                |
| `stop_editor(handle)`                                                                                                              | SIGTERM to the process group this toolkit started, SIGKILL after the timeout                                                                                                                                                                                                    | never used on an editor you did not start                                                                                                     |
| `in_commandlet()`                                                                                                                  | `unreal.SystemLibrary.get_command_line()` contains `-run=`                                                                                                                                                                                                                      |                                                                                                                                               |
| `result(obj)`, `args()`, `to_jsonable(x)`                                                                                          | pure Python inside the job (UObject to `get_path_name()`, structs to x/y/z, enums to their name)                                                                                                                                                                                |                                                                                                                                               |

## 3. `ue_review` (looking and checking)

| Toolkit call                                                                                  | Engine call, command or cvar                                                                                                                                                                                                                                                                                                                                                                                                                                          | Notes                                                                                                                              |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `set_camera(loc, rot, fov)`                                                                   | `UnrealEditorSubsystem.set_level_viewport_camera_info(Vector, Rotator)`; `LevelEditorSubsystem.set_level_viewport_fov(fov)` (5.8)                                                                                                                                                                                                                                                                                                                                     | Rotator built by keyword (positional order roll, pitch, yaw [verify])                                                              |
| `screenshot(path, w, h, camera=None)`                                                         | `unreal.AutomationLibrary.take_high_res_screenshot(w, h, path, camera=..., capture_hdr=..., delay=..., force_game_view=True)`; fallback `SystemLibrary.execute_console_command(world, 'HighResShot WxH filename="path"')`                                                                                                                                                                                                                                             | returns at once; the file lands after ticks; keywords [verify]                                                                     |
| `wait_screenshot(req)` (latent)                                                               | `yield` until `task.is_task_done()` and the file exists; falls back to the newest file in `Saved/Screenshots`                                                                                                                                                                                                                                                                                                                                                         | pattern of Epic's Python automation-test sample ("Screenshot Support")                                                             |
| `screenshot_views(views, out_dir)`                                                            | `set_camera`, 5 settle ticks, `screenshot`, `wait_screenshot` per bookmark                                                                                                                                                                                                                                                                                                                                                                                            | fixed bookmarks make iterations comparable                                                                                         |
| `camera_views_from_actors(tag="AgentView")`                                                   | `EditorActorSubsystem.get_all_level_actors()` filtered to `CameraActor` with that actor tag                                                                                                                                                                                                                                                                                                                                                                           | bookmarks saved in the level                                                                                                       |
| `render_still(seq_or_camera, out_dir, preset)`                                                | new `MoviePipelineQueue`, `queue.allocate_new_job(MoviePipelineExecutorJob)`; graph preset: `job.set_graph_preset(MovieGraphConfig)` and `job.get_or_create_variable_overrides(preset)` for `OutputDirectory`, `CustomStartFrame`, `CustomEndFrame`; legacy: `job.set_configuration(MoviePipelinePrimaryConfig)` and `MoviePipelineOutputSetting`; `MoviePipelinePIEExecutor().execute(queue)`, callback on `on_executor_finished_delegate` writes `render_done.json` | executor kept referenced; a camera gets a one-frame `LevelSequence` with a camera cut (end frame exclusive); variable API [verify] |
| `build_render_command(eng, up, map, seq, config)`                                             | `UnrealEditor-Cmd <uproject> <map> -game -LevelSequence=<seq> -MoviePipelineConfig=<preset or saved queue> -windowed -resx= -resy= -log -unattended -notexturestreaming`                                                                                                                                                                                                                                                                                              | MRQ command line doc                                                                                                               |
| `image_checks`, `image_verdict`, `compare`, `contact_sheet`, `review_images`, `wait_for_file` | no engine call: pure Python on PNG or EXR files (Rec. 709 luma, clipped and crushed shares, EV-like mean, center vs surround, `all_white`, `all_black`, `uniform`)                                                                                                                                                                                                                                                                                                    | ran offline                                                                                                                        |

## 4. `ue_audit`, `ue_stat`, `ue_env`

| Toolkit call                                          | Engine call, command or cvar                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Notes                                                      |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| `ue_audit.audit_assets(paths, rules, profile)`        | `EditorAssetSubsystem.list_assets`, `does_directory_exist`, `load_asset`; per class: Texture2D `blueprint_get_size_x/y`, `compression_settings`, `srgb`, `lod_group`, `virtual_texture_streaming`; StaticMesh `get_num_lods`, `get_num_triangles(0)`, `nanite_settings.enabled`, `StaticMeshEditorSubsystem.get_simple_collision_count`, `body_setup.collision_trace_flag`, `static_materials`; MaterialInstanceConstant `parent`, `get_base_material`, `static_parameters`; Material `blend_mode`, `material_domain`, `translucency_pass`, Custom expressions through `MaterialEditingLibrary.get_expressions` [verify]; `get_asset_filename_length_for_cooking`, `find_package_referencers_for_asset` | read-only, never saves                                     |
| `ue_audit.evaluate`, `build_report`, `verdict`        | pure Python rules on the collected facts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ran offline                                                |
| `ue_stat.parse_stat_unit`                             | text of the `stat unit` console command (Frame, Game, Draw, GPU, RHIT)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |                                                            |
| `ue_stat.parse_csv_profile`, `csv_frames`             | CSV from `-csvprofile` or console `csvprofile start` / `csvprofile stop` (`Saved/Profiling/CSV`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | column names [verify]                                      |
| `ue_stat.parse_jsonl`, `trace_frames`, `timer_totals` | JSONL written by 5.8 `TraceQuery` from a `.utrace` captured with `-trace=default -tracefile=<abs>` (lean) or `-trace=default,task -statnamedevents` (rich, about 20% overhead, C-AjCqjKRSs [00:11:27])                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | schema undocumented: `sniff_schema` first                  |
| `ue_stat.budget_check(frames, 16.67)`, `bound_by`     | pure Python: mean, p50, p95, p99, max, share over budget, hitches, bounding thread                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |                                                            |
| `ue_env.find_engine()`                                | `/Users/Shared/Epic Games/UE_5.x/Engine/Binaries/Mac/UnrealEditor.app/Contents/MacOS/UnrealEditor`, `UnrealEditor-Cmd` if present, `Engine/Build/BatchFiles/RunUAT.sh`, `Engine/Build/Build.version` (`UE_ROOT` overrides)                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Launcher path [verify]                                     |
| `ue_env.preflight()`                                  | `sw_vers -productVersion`, `sysctl -n machdep.cpu.brand_string`, `sysctl -n hw.memsize`, `xcodebuild -version` against Epic's 5.8 window                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ran on this Mac                                            |
| `ue_env.read_ini_value(ini, section, key)`            | reads `Config/DefaultEngine.ini`, e.g. `[/Script/Engine.RendererSettings] r.Substrate`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Default layer only, no Base merge                          |
| `ue_env.enable_plugins(up, names)`                    | edits the `.uproject` `Plugins` array after a timestamped backup; restart needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | plugin identifiers such as `ModelContextProtocol` [verify] |

## 5. `ue_remote` (live channels)

| Toolkit call                                                 | Engine call, command or cvar                                                                                                                                                                                                                                                  | Notes                                                                                                |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `RemoteControl().info()`                                     | `GET http://127.0.0.1:30010/remote/info`                                                                                                                                                                                                                                      | server off until `WebControl.StartServer`                                                            |
| `.describe(path)`                                            | `PUT /remote/object/describe {"objectPath"}`                                                                                                                                                                                                                                  |                                                                                                      |
| `.get_property(path, name)` / `.set_property(path, name, v)` | `PUT /remote/object/property` with `"access": "READ_ACCESS"` / `"WRITE_TRANSACTION_ACCESS"` (undoable)                                                                                                                                                                        | public EditAnywhere properties, C++ names                                                            |
| `.call(path, fn, params)`                                    | `PUT /remote/object/call {"objectPath", "functionName", "parameters", "generateTransaction"}`                                                                                                                                                                                 | refused by default in 5.8 unless allowed in Remote Control project settings (setting names [verify]) |
| `.batch(requests)`, `.search_assets(...)`                    | `PUT /remote/batch`, `PUT /remote/search/assets`                                                                                                                                                                                                                              |                                                                                                      |
| `.console(cmd)`                                              | `/remote/object/call` on `/Script/Engine.Default__KismetSystemLibrary`, function `ExecuteConsoleCommand`                                                                                                                                                                      | needs the 5.8 remote-call permission                                                                 |
| `PythonRemote().open()/exec(code)/call(module, fn)`          | Python Remote Execution (Project Settings > Plugins > Python > Enable Remote Execution): UDP multicast discovery 239.0.0.1:6766, TCP command channel on 127.0.0.1:6776, commands `ExecuteFile`, `ExecuteStatement`, `EvaluateStatement`; prefers Epic's `remote_execution.py` | defaults [verify]; `call` sends code that imports `scripts/<module>` and prints `UE_RESULT`          |
| `McpHttp().health()`                                         | JSON-RPC `initialize`, `notifications/initialized`, `tools/list` by HTTP POST to `http://127.0.0.1:8000/mcp` (SSE replies parsed)                                                                                                                                             | health check only; the agent's own MCP client does the work                                          |
| `loopback_only(port)`                                        | `lsof -nP -iTCP:<port> -sTCP:LISTEN`: True when every listener is 127.0.0.1 or ::1                                                                                                                                                                                            |                                                                                                      |
